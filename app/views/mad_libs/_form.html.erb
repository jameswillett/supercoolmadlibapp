<%= form_with(model: mad_lib, local: true) do |form| %>

  <div class="field">
    <%= form.label "Enter you madlib template below" %>
    <%= form.text_area :text, id: :mad_lib_text %>
  </div>

  <div class="actions">
    <%= form.button :Create %>
  </div>
<% end %>
